<div id ="Principal">
  <h1>Bienvenido a mi Blog</h1>
  <h3>¡Aquí podras hacer "Posts" y "Tags"!</h3>
  <h4>Y en un futuro, quiza hasta compartirlos con tus amigos</h4>
    <button id="n_Post">Nuevo Post</button>
    <button id="a_posts">Posts</button>
    <button id="f_posts">Buscar un post</button>
    <button id="a_tags">Tags</button>
  <!-- Figura hacer un post -->
  <div id="m_posts">
    <form id="form1" action = "/make/post" method="post">
       <%= erb :makepost, layout: true %>
    </form>
  </div>
  <!-- Figura para todos los posts -->
  <div id="all_posts">
      <%= erb :allposts, layout: true %>
   </div>
  <!-- Figura de un post -->
  <div id="find_posts">
       <%= erb :findpost, layout: true %>
  </div>

  <div id="all_tags">
      <%= erb :findtags, layout: true %>
   </div>

  <h6>*Esta pagina no es una simulación de un blog real, no te emociones.</h6>
</div>

  
